services:
  yolo-api:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./models:/models
    environment:
      - MODEL_PATH=/models/best.pt
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
